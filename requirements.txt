# Exact deterministic reference environment for the current AI Engineering study edition.
# Python itself is pinned separately in .python-version (canonical: 3.13.5).
-r companion/requirements-reference.txt

# Dataset/lab helpers used by the principal companion exercises.
ucimlrepo>=0.0.7,<1
openpyxl>=3.1,<4
